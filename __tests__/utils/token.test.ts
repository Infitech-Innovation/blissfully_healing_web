// import { decodeRetreatToken, encodeRetreatToken } from '@/utils/token';

// describe('retreat token helpers', () => {
//   it('decodes a token created from a number retreat id', () => {
//     const originalData = { retreatId: 4, retreatName: 'Sunrise Silence Weekend' };
    
//     // Encode the mock payload into a token string
//     const token = encodeRetreatToken(originalData);

//     // Decode it back to check for functional accuracy
//     const decoded = decodeRetreatToken(token);

//     expect(decoded).toEqual(originalData);
//   });
// });